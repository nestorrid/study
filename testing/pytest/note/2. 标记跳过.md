# 跳过测试用例

* 无条件跳过:

  * ```python
    @pytest.mark.skip(reason='...')
    ```

* 有条件跳过:

  * ```python
    @pytest.mark.skipif(condition, reason='...')
    ```

  

## 跳过模块

通过`pytestmark`变量进行模块级的跳过, 变量名固定

```python
pytestmark = pytest.mark.skip
```

